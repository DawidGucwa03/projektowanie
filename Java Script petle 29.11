<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <script>

        // Zadanie 1. gwiazdka
        
        // const a = prompt('Wprowadź wysokość: ');
        // const b = prompt('Wprowadź szerokosć: ');

        // for (let i = 0; i < a; i++) {
        //     document.write('# ');
        //     for (let l = 1; l < b - 1; l++) {
        //         if (i >= 1 && i < a - 1) {
        //             document.write(' ');
        //         } else
        //             document.write('# ');
        //     }
        //     document.write('<br>');
        // }

        // Zadanie 2.
        //??

        // Zadanie 3. NWD

        // const a = prompt('Podaj a: ');
        // const b = prompt('Podaj b: ');

        //         while(a!=b)
        //             if(a>b){
        //                 a = a - b;
        //             }else{
        //                 b = b - a;
        //             }

        // Zadanie 4. silnia z liczby n

        // const n = parseInt(prompt('Wrpowadź n: '));
        // if (n < 0) {
        //     document.write('Error! Liczba musi być dodatnia!');
        // } else if (n === 0) {
        //     document.write(`Silnia z liczby ${n} wynosi: 1.`);
        // } else {
        //     let fact = 1;
        //     for (i = 1; i <= n; i++) {
        //         fact *= i;
        //     }
        //     document.write(`Silnia z liczby ${n} wynosi: ${fact}.`);
        // }

        // Zadanie. 5 Liczby podzielne przez 2

       </script>
      <!-- <form action="" method="POST">
        <input type="number" name="liczba1" placeholder="wpisz pierwszą liczbę">
        <input type="number" name="liczba2" placeholder="wpisz drugą liczbę">
        <input type="submit" value="wynik">
      </form>
      <div class="wynik"></div>

      <script>
        const formularz = document.querySelector('form');
        const skutek = document.querySelector('div.wynik');

        formularz.addEventListener('submit', (e) => {
            e.preventDefault();
            const pocz_przedzialu = document.querySelector('input[name="liczba1"]').value;
            const kon_przedzialu = document.querySelector('input[name="liczba2"]').value;

            let i = pocz_przedzialu;
            while (i < kon_przedzialu) {
                if (i % 2 == 0) div.textContent += i + ' ';
                i++;
            }
        });
      </script> -->
      <script>

        // Zadanie 6. Liczba N pierwsza
        
        const n = prompt('Wprowadź n; ');

        if (n < 2) {
            document.write('Wprowadzona liczba nie jst pierwszą!')
        } else {
            for (int i = 2; i * i <= n; i++) {
                if (n % i == 0)
            }
        }
      </script>
   </body>
</html>
